export default function stringPermutations(str) {
  // given an input string return all the possible permutations of the string ie. "abb" -> ["abb", "bab", "bba"]
  // Do not return a permutation twice. ie a string of three unique characters would normally return 6 unique permutations
  //return permutations;
}
